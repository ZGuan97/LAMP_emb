# Coding Log 2026-05-19 22:18

本次记录对应 fragment-based DMET 的 bath orbital construction 初步实现。目标是让 `embed_sim/fragment.py` 中的 `FDMET.build()` 不再只停留在 fragment-local CAHF/ROHF reference 构造，而是开始从每个局部 reference density 生成 bath orbitals，并输出可供现有 SSDMET Hamiltonian projection 继续使用的全局 AO coefficient。

## 本次完成

1. 在 `FragmentReference` 中新增 `local_to_global_idx`。
   - 该列表记录每个局部 fragment AO 对应的母体 molecule 全局 AO index。
   - 后续局部 bath orbital 映回全局 AO basis 时使用该映射。

2. 实现 `fragment_bath_from_fragment_density()`。
   - 输入为 fragment-local AO density。
   - 若 density 是 spin-resolved 形式，则先合并 alpha/beta density。
   - 复用模块级 `ssdmet.lowdin_orth(..., preserve_imp=True)` 做固定 impurity 的 Lowdin 正交化。
   - 在 `local_imp_idx + local_fragment_idx` 子空间中只对 fragment/environment density block 做自然轨道分解。
   - 选出的 bath orbital 仍按 SSDMET 的环境自然占据数阈值规则确定。
   - 不对 impurity+bath embedded block 做自然轨道分解，避免 bath orbital 与 fixed impurity orbital 混合。

3. 实现局部 bath orbital 到母体 AO basis 的嵌回。
   - 新增 `fragment_orbitals_to_global_orbitals()`。
   - 局部 AO coefficient 被放回长度为 `mol.nao` 的全局 AO coefficient，其他 AO 分量为 0。

4. 实现多个 fragment bath 的全局合并和正交化。
   - 新增 `_orthogonalize_global_bath()`。
   - 在母体 AO overlap metric 下先投影掉全局固定 impurity 分量。
   - 再对 bath block 的 overlap matrix 对角化，按显式 `svd_tol` 过滤线性相关方向。

5. 实现 embedded cluster basis 的补空间构造。
   - 新增 `_complete_global_orbitals()`。
   - `es_orb = [fixed impurity orbitals, merged fragment bath orbitals]`。
   - `fv_orb` 当前定义为 impurity+bath 的全局正交补空间。
   - `fo_orb` 当前保守设为空。

6. 让 `FDMET.build()` 完整进入后续 SSDMET 流程。
   - 逐个构造 fragment reference。
   - 从每个 fragment reference density 构造 bath。
   - 合并 bath 并设置 `es_orb`、`fo_orb`、`fv_orb`、`es_occ`、`nfo`、`nfv`、`nes`。
   - 调用现有 `make_es_int1e()` 和 `make_es_int2e()` 生成嵌入空间积分。
   - 返回 `self.CAHF()` 生成的 embedded CAHF 对象。

7. 更新 `fragment.md`。
   - 记录当前 fragment-local bath orbital construction 的实现流程。
   - 明确当前 `fo_orb`、`fv_orb` 和 `es_occ` 的近似约定。
   - 说明 fragment bath 不保留 fragment-local natural occupation 作为后续计算输入。

8. 重构 impurity-preserving Lowdin 入口。
   - 将 `preserve_imp` 逻辑统一放到模块级 `ssdmet.lowdin_orth()`。
   - `SSDMET.build()`、`DFSSDMET.build()` 和 fragment bath construction 都直接调用该模块级函数。
   - 移除 fragment 中临时构造 `orth_context` 的做法。
   - `ssdmet.lowdin_orth()` 只负责返回 `(caolo, cloao)`；density 到正交轨道表示的变换 `ldm = cloao @ dm @ cloao.T` 由调用方显式完成。
   - 保持旧的 `ssdmet.lowdin_orth(mol, ovlp=s)` 调用方式可用。

9. 清理 fragment bath 构造接口。
   - 将低层函数 `bath_from_fragment_density()` 改名为 `fragment_bath_from_fragment_density()`，明确它属于 fragment bath 路径。
   - 删除重复的高层 `build_fragment_baths()` functional API。
   - 将 `FDMET.build_fragment()` 合并进 `FDMET.build()`，类接口只保留一个 build 入口。

10. 新增嵌入空间 CAHF。
   - 在 `FDMET` 中新增 `CAHF()` 方法，结构仿照 `SSDMET.ROHF()`。
   - `CAHF()` 接收与 `fragment_scf_options` 一致的常用参数，例如 `ncas`、`nelecas`、`cahf_spin`、`max_cycle`、`level_shift` 和 `diis`。
   - `FDMET.build()` 现在将 `self.es_mf` 设置为 `self.CAHF(**self.fragment_scf_options)`。
   - 对 embedded CAHF 的 RDIIS，若 `rdiis_imp_idx` 是 AO label/pattern 形式，则改用嵌入空间中的 impurity block index。

## 当前重要约定

1. bath orbital 的构造方法和 SSDMET 一致。
   - 仍然对环境 density block 做自然轨道分解。
   - 占据数在 `[threshold, 2-threshold]` 内的环境自然轨道作为 bath。

2. impurity preserve 方法是固定步骤。
   - `FDMET` 的 fragment bath 构造复用现有模块级 `ssdmet.lowdin_orth(..., preserve_imp=True)`。
   - 没有引入第二套 impurity-preserving orthogonalization 实现。

3. fragment bath 最终以母体 molecule 的全局 AO coefficient 表示。
   - 这样后续可以继续复用现有 `make_es_int1e()`、`make_es_int2e()`、`avas()`、`total_cas()` 和 SISO 相关接口。

4. 当前不从 fragment-local occupied orbitals 直接构造全局 frozen occupied space。
   - 多个 fragment reference 都包含同一个 impurity，且不同局部 occupied spaces 不能直接拼接。
   - 若直接合并可能导致电子重复计数。
   - 因此本次实现中 `fo_orb` 暂时为空。

5. 当前 `es_occ` 是 embedded space 内的 Aufbau-like 初猜。
   - 它用于构造 embedded CAHF 的初始密度。
   - 它不是由全局 reference density 对角化得到的自然占据。

## 轨道合并与 FO 保留策略

当前 fragment bath construction 的轨道合并分成三层：局部 fragment 内部构造、局部到全局 AO basis 的嵌回、以及多个 fragment bath 的全局正交化。

1. 单个 fragment 内部构造 bath。
   - 对第 `f` 个 fragment，先构造并运行 `impurity + ligand_f` 的局部 reference，得到 `frag_ref.mf`。
   - 用 `ssdmet.mf_or_cas_make_rdm1s(frag_ref.mf)` 得到 fragment-local AO density。
   - 在 `fragment_bath_from_fragment_density()` 中，先用模块级 `ssdmet.lowdin_orth(..., preserve_imp=True)` 做固定 impurity 的正交化。
   - 显式计算局部正交轨道表示中的 density：

```python
ldm = cloao @ dm @ cloao.T
```

   - 然后只取 `local_imp_idx + local_fragment_idx` 这部分局部轨道中的 fragment/environment density block，并对该 block 做自然轨道分解。
   - 这里不调用 `ssdmet.build_embeded_subspace()`，因为该函数会继续对 impurity+bath block 对角化，得到 embedded-space natural orbitals，从而把 fixed impurity 和 bath 混合在一起。
   - 当前 fragment 路径只使用 environment natural orbitals，并按标准 SSDMET 的环境自然占据数规则把局部 environment 轨道分成 bath、fragment-local FO 和 fragment-local FV。
   - 当前 FDMET 只使用其中的 `bath_orb`；fragment-local FO/FV 不进入全局 FO/FV，fragment-local natural occupation 也不再计算或保存。

2. 局部 bath 嵌回母体 AO basis。
   - `fragment_bath_from_fragment_density()` 返回的 `bath_orb` 仍然在该 `fragment_mol` 的局部 AO basis 中。
   - `FragmentReference.local_to_global_idx` 记录每个局部 AO 对应的母体 molecule 全局 AO index。
   - `fragment_orbitals_to_global_orbitals()` 使用这个映射将局部 coefficient 嵌回长度为 `mol.nao` 的全局 AO coefficient：

```python
global_orb[global_ao, :] = local_orb[local_ao, :]
```

   - 对不属于该 fragment molecule 的母体 AO，系数保持为 0。
   - 这一步只改变表示方式，不做正交化，也不改变该 fragment bath 的线性组合。

3. 多个 fragment bath 的全局合并。
   - 每个 fragment 都给出一组已经嵌回母体 AO basis 的 bath block：

```python
B_1, B_2, ..., B_nf
```

   - 当前实现先将它们横向拼接为 `raw_bath`。
   - 因为不同 fragment 的 bath 是分别构造的，拼接后一般不保证彼此正交，也不保证完全不含全局 impurity 分量。
   - `_orthogonalize_global_bath()` 在母体 AO overlap metric `S` 下处理这个 `raw_bath`。
   - 首先投影掉 fixed impurity 分量：

```python
raw_bath = raw_bath - C_imp @ (C_imp.T @ S @ raw_bath)
```

   - 然后构造 bath overlap：

```python
S_bath = raw_bath.T @ S @ raw_bath
```

   - 对 `S_bath` 对角化，只保留 eigenvalue 大于 `svd_tol` 的方向，并乘以 `eig^-1/2` 做归一化。
   - 输出的 merged bath 满足全局 AO metric 下的正交归一，并且与 fixed impurity 正交。

4. 构造最终 embedded/frozen virtual 空间。
   - `_complete_global_orbitals()` 首先用同一个 preserve-impurity Lowdin 规则生成全局固定 impurity basis。
   - 然后组合：

```python
es_orb = [fixed impurity orbitals, merged fragment bath orbitals]
```

   - `fv_orb` 定义为 fixed impurity + merged bath 在全局正交 AO 表示中的正交补空间。
   - 因此最终 `[fo_orb, es_orb, fv_orb]` 在 AO overlap metric 下应当形成一组正交归一轨道，其中当前 `fo_orb` 为空。

5. 当前如何保留 `fo_orb`。
   - 当前默认方案是不从 fragment-local FO 轨道构造全局 FO。
   - 代码中 `_complete_global_orbitals()` 返回：

```python
fo_orb = np.zeros((mol.nao, 0))
```

   - 因此：

```python
nfo = 0
N_emb = N_total
```

   - 这样做的原因是多个 fragment reference 都包含同一个 impurity，fragment-local FO 轨道之间既不正交，也不能直接解释为同一组全局 occupation=2 的自然轨道。
   - 如果直接拼接 fragment-local FO，会破坏正交性并重复计数电子。
   - 如果先正交化再当作 FO，则正交化后的轨道不再对应原来每个 fragment density 中 occupation=2 的自然轨道，因此 `2 * nfo` 的电子数解释不可靠。
   - 因此当前 `fo_orb` 被有意保留为空矩阵，而不是“还没实现”的占位。它代表当前 fragment-DMET 默认不冻结任何 occupied orbital。
   - 后续若需要压缩电子数，应新增一个独立方案来定义全局 FO，而不是复用 fragment-local FO。

## 验证

1. 语法检查：

```bash
python -m py_compile embed_sim/fragment.py
```

结果：通过。

2. `pyscf` 环境导入检查：

```bash
source /opt/anaconda3/etc/profile.d/conda.sh && conda activate pyscf
python - <<'PY'
from embed_sim import fragment
print(fragment.FDMET)
PY
```

结果：通过。

3. 最小 H2 fragment ROHF sanity check。
   - `FDMET.build()` 成功返回。
   - `nes=2, nfo=0, nfv=0`。
   - `es_orb` 和完整 orbital block 在 AO metric 下的正交误差约为 `1e-15`。

4. 四氢链中的双 fragment ROHF sanity check。
   - `FDMET.build()` 成功返回。
   - `nes=3, nfo=0, nfv=1`。
   - `[fo_orb, es_orb, fv_orb]` 在 AO metric 下的正交误差约为 `1e-15`。

未运行完整 `examples/fragment_dmet.py` 的 CoSH4 CAHF 示例，因为该例子计算量较大，且本次主要验证 bath construction 的矩阵形状、映射和正交性。

## FO 轨道与整数电子数问题

传统 DMET 中只有一组来自全局 reference density 的正交归一 FO 轨道，因此 FO 上的电子数可以直接写成 `2 * nfo`。embedded space 的电子数为总电子数减去 FO 电子数，仍然是整数，可以直接用于后续 ROHF、CASCI 或 CASSCF。

当前 fragment 实现的困难在于：不同 fragment 会各自产生 fragment-local FO 候选轨道，这些轨道嵌回母体 AO basis 后并不正交，并且每个 fragment reference 都包含同一个 impurity。它们不能简单拼接；若强行正交化，得到的轨道也不再对应原来 fragment density 中 occupation=2 的自然轨道，因此无法保证 FO 上电子数仍可解释为整数 `2 * nfo`。

目前记录三种可能方案。当前决定采用方案 1 作为默认路线，因为它不需要从 fragment-local FO 轨道推断全局整数电子数，语义最干净，代码实现也最简单。

1. 不构造 FO。
   - 这是当前代码采用的默认方案。
   - `fo_orb = []`。
   - `fv_orb` 定义为 fixed impurity + merged bath 的全局正交补空间。
   - `nfo = 0`，embedded electron number 等于全体系电子数。
   - 优点是避免重复计数和非整数 FO 电子数问题。
   - 缺点是不能利用 frozen occupied space 做进一步电子数压缩。
   - 该方案要求 `impurity + bath` 轨道数足够容纳全体系电子数；若容量不足，代码应直接报错。

2. 从全局近似 density 重新定义 FO。
   - 不直接合并 fragment-local FO 轨道。
   - 先将 fragment-local density 映回母体 AO basis。
   - 为避免 impurity 重复计数，优先只拼装 ligand/environment block 的 density。
   - 在 fixed impurity + merged bath 的正交补空间中对该近似 environment density 做自然轨道分解。
   - 自然占据数接近 2 的全局正交轨道定义为 FO，接近 0 的定义为 FV。
   - 这样可以恢复 `N_FO = 2 * nfo` 和整数 embedded electron number。
   - 这是当前认为最接近传统 SSDMET 逻辑的推荐后续方向。

3. 只冻结明确的全局 core 轨道。
   - 不试图恢复所有 fragment-local FO。
   - 只选择远离 impurity+bath、自然占据稳定接近 2、且可通过 AO label、壳层、能量或 impurity 成分判断为深层 core 的轨道。
   - 优点是物理保守且电子数仍为整数。
   - 缺点是压缩效率有限。

方案 2 和方案 3 暂时保留为后续可选扩展，不作为当前 fragment bath construction 默认路径的一部分。

## 剩余问题

1. 若后续需要诊断 fragment bath 强弱，可再显式加入可选的 fragment environment natural occupation 输出。
2. 是否需要限制总 bath 数量，例如按 occupation 距离、fragment 权重或 impurity 耦合强度排序。
3. 如何定义全局 frozen occupied space 仍未解决。
4. 当前 fragment-local SCF 仍只在截断 molecule 上运行，没有加入其他 ligand 的 point charge、embedding potential 或全局投影势。
5. 当前 `es_occ` 只是 embedded CAHF 初猜，后续若要与标准 SSDMET 的自然占据语义完全一致，需要设计 fragment-local density 到全局 embedded density 的合并规则。
6. 还需要在 CoSH4/CAHF 实例上检查收敛性、bath 数量、AVAS 和后续 SISO 工作流。

## 2026-05-24 更新：FO/FV 分类修正与 embedded FO

本轮主要修正了 fragment bath 合并后 FO/FV 语义不清的问题。之前的实现只保留 fragment-local bath，并用 `_complete_global_orbitals()` 在全局 preserve-impurity basis 中取 bath 的正交补作为 `fv_orb`。CoSH4 单 fragment 诊断显示，这个所谓 `fv_orb` 上有约 6 个电子：

```text
density trace on kept-FV block = 6.000000000004
max occupied/active MO FV weight = 0.982...
```

这说明当时的 `fv_orb` 实际是 `fragment FO + true FV` 的混合补空间，而不是真正的 CAHF virtual 子空间。问题根源是 `fragment_bath_from_fragment_density()` 返回了 `bath_orb, fo_orb, fv_orb`，但 `FDMET.build()` 只取了 `bath_orb`，把 fragment-local FO 信息丢掉了。

### 当前采用的修正

1. 保留 fragment-local FO 信息。
   - `FDMET.build()` 现在接收 `fragment_bath_from_fragment_density()` 返回的 `fo_orb`。
   - `fo_orb` 和 `bath_orb` 一样，通过 `local_to_global_idx` 从 fragment-local AO basis 嵌回母体 AO basis。

2. 全局 FV 的定义改为 `bath + FO` 的正交补。
   - `_complete_global_orbitals()` 现在接收 `fo_orb`。
   - 在全局 preserve-impurity basis 的 environment block 中，先整理 bath 方向，再将 FO 对 bath 正交化。
   - 最终 `fv_orb` 是 `bath + FO` 在 environment space 中的正交补，因此不会再把 occupied FO 误标成 FV。

3. FO 不作为 frozen occupied orbital，而是并入 embedded space。
   - 当前实际使用的 embedded space 为：

```text
es_orb = impurity + bath + embedded FO
```

   - 因为 FO 被放入 ES，最终：

```text
fo_orb = []
nfo = 0
N_emb = N_total
```

   - 日志中新增 `number of occupied environment orbitals embedded`，用于记录有多少 fragment-local FO 被放进了 ES。
   - 若 `keep_fv_orbitals=True`，测试模式下 embedded space 进一步变为：

```text
es_orb = impurity + bath + embedded FO + true FV
fv_orb = []
```

4. `keep_fv_orbitals=True` 的 FV 诊断同步更新。
   - FV block 的起点现在是：

```python
fv0 = mydmet.nimp + mydmet.nbath + mydmet.nembedded_fo
```

   - 这样诊断只检查真正的 kept FV，不会再把 embedded FO 误当成 FV。

### RDIIS impurity label 映射

embedded CAHF 的基函数不是原始 AO label，而是 `impurity + bath + embedded FO (+ FV)`。之前如果在 `fragment_scf_options` 中传：

```python
'rdiis_imp_idx': ['Co.*d']
```

embedded CAHF 无法直接解析这个 AO label，只能退回整个 impurity block，并打印 warning。

现在 `FDMET` 保存了 impurity parent AO 到 embedded impurity block 的映射：

```python
impurity_ao_to_embedded_idx
impurity_aolabel_to_embedded_idx
```

并提供类似 PySCF 的查询接口：

```python
mydmet.search_impurity_ao_label('Co.*d')
mydmet.search_impurity_ao_label(['Co.*d'])
```

因此 embedded CAHF 的 RDIIS 可以继续接受类似 PySCF 的 AO label 输入，并自动转换为 embedded-space integer indices。

### FragmentReference 精简

`FragmentReference` 中原来保存的 `atom_ids` 和 `global_idx` 只在 `_make_fragment_mol()` 内部用于构造局部 molecule 和检查 AO/atom 映射。后续 bath 构造和轨道合并并不使用它们。

现在 `FragmentReference` 只保留后续必要字段：

```python
mol
mf
local_to_global_idx
local_imp_idx
local_fragment_idx
```

其中：

1. `local_to_global_idx` 用于将 fragment-local bath/FO 轨道嵌回母体 AO basis。
2. `local_imp_idx` 用于 fragment-local preserve-impurity Lowdin 和 density block 切分。
3. `local_fragment_idx` 用于对 ligand/environment density block 做自然轨道分解。

### 验证

已完成以下轻量检查：

```bash
python -m py_compile embed_sim/fragment.py examples/fragment_dmet.py
```

通过。

小 H2 fragment build sanity check 通过，确认 `FragmentReference` 精简后 build 路径仍可运行。

小体系 RDIIS label 解析测试通过，`rdiis_imp_idx='0 H.*'` 可解析为 embedded-space index `[0]`，不再触发 fallback warning。
