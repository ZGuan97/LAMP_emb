# Coding log 2026-05-30 15:35

本次主要解决 fragment-DMET 中 embedded-space CAHF 初猜过差的问题。之前 `FDMET.CAHF()` 虽然在 `build()` 阶段已经跑完 fragment-local CAHF 并构造了 fragment bath，但 embedded CAHF 初猜仍基本是 embedded basis 中的 Aufbau-like 对角密度；在 `examples/fragment_dmet.py` 中调用 `mydmet.es_mf.run()` 时还会走 PySCF 默认 `minao` 初猜。CoSH4 日志显示旧路径 `init E = -1724.1657`，随后 500 cycle 仍未收敛。

## 本次实现

1. 在 `embed_sim/fragment.py` 中新增 fragment density 拼接辅助函数：
   - `_fragment_density_to_parent_density(mol, fragment_refs)`
   - `_rescale_density_trace(dm, nelectron)`

2. 新增 `FDMET` 构造参数：
   - `embedded_init_guess`
   - `embedded_active_aolabels`

3. 新增 `embedded_init_guess="fragment_density"` 路径。
   - 从每个 fragment-local CAHF reference 提取 AO density。
   - 将 density 嵌回母体 AO basis。
   - ligand-fragment 内部 block 和 impurity-ligand cross block 来自对应 fragment。
   - 重复的 impurity block 在所有 fragment reference 之间平均。
   - 不同 ligand fragment 之间的 cross block 置为 0。
   - 将拼接 density 投影到 embedded basis，并把 trace rescale 到 embedded electron number。

4. 实现 Co 3d 固定 active 的 embedded CAHF 初猜。
   - `embedded_active_aolabels="Co 3d"` 只在 fixed impurity block 内解析。
   - CoSH4/def2-TZVP 中 `Co 3d` 选择 5 个 embedded indices `[18, 19, 20, 21, 22]`。
   - 这 5 个 orbital 直接作为 CAHF active orbitals。
   - 不使用 `Co.*d` 作为 active label，因为它会选到 Co 3d/4d/5d/6d 共 20 个 d 型函数。

5. 对 active 之外的环境 block 做 density 对角化。
   - `env = embedded space - active_idx`
   - 对 `D_env` 对角化并按 occupation-like eigenvalue 从大到小排序。
   - 前 `ncore = (nelectron - nelecas) // 2` 个作为 core。
   - 剩余作为 virtual。
   - 最终 embedded CAHF 初猜为：

```text
mo_coeff = [environment core natural orbitals, fixed active AO, environment virtual natural orbitals]
mo_occ   = [2, ..., 2, nelecas/ncas, ..., nelecas/ncas, 0, ..., 0]
```

6. `FDMET.CAHF()` 现在会把 `es_mf.get_init_guess` 指向 `self.es_dm`，因此 `mydmet.es_mf.run()` 会真正使用 fragment-density 初猜，而不是回退到 PySCF 的 `minao`。

7. 更新 `examples/fragment_dmet.py`：
   - 加入 `embedded_init_guess='fragment_density'`
   - 加入 `embedded_active_aolabels='Co 3d'`
   - 保留 fragment/embedded RDIIS 的 `rdiis_imp_idx=['Co.*d']`

这里需要区分两个概念：`embedded_active_aolabels='Co 3d'` 是 CAHF active 初猜定义；`rdiis_imp_idx=['Co.*d']` 是 RDIIS 监控区域。测试中如果把 RDIIS 也改成 `Co 3d`，fragment-local CAHF 反而不收敛，因此当前示例保留原来的 `Co.*d` RDIIS 区域。

8. 更新 `fragment.md`，记录 fragment-density embedded CAHF initial guess 的算法、CoSH4 数值和注意事项。

## CoSH4 测试结果

运行：

```bash
conda run -n pyscf python examples/fragment_dmet.py
```

关键输出：

```text
fragment-density embedded guess: raw trace 95.0346402911, scale 1.02068045612
fragment-density embedded guess: active Co 3d -> [18, 19, 20, 21, 22]
fragment-density embedded guess: env core min 1.12448784038, first virtual 0.379031865686
initial guess = fragment_density
init E= -3661.55677969186
converged SCF energy = -3669.30768115344
```

embedded CAHF 在第 73 cycle 收敛。相比旧的 `minao` 初猜，初始能量从约 `-1724` 改善到 `-3661.56`，并且不再出现 500 cycle 不收敛的问题。

命令最终 exit code 为 1，是因为 `examples/fragment_dmet.py` 中原有诊断段保留了裸 `raise`；embedded CAHF 本身已经收敛。

语法检查通过：

```bash
conda run -n pyscf python -m py_compile embed_sim/fragment.py examples/fragment_dmet.py
```

## 当前假设

1. 不同 ligand fragment 之间的 overlap/cross density 在拼接初猜时近似为 0。
2. 重复出现的 impurity density block 使用 fragment average。
3. fragment-projected density 只用于构造 embedded CAHF 初猜，不作为物理 density 直接解释。
4. active orbitals 由 fixed impurity block 中的 AO label 显式指定；CoSH4 当前使用 `Co 3d`。
5. 拼接 density 可能不是严格 N-representable，因此只使用其环境 eigenvalue 排序，而不直接使用这些 eigenvalue 作为 CAHF occupation。

## 剩余问题

1. 是否需要把 fragment-density 初猜推广到其他金属或 f-shell 体系，例如 `Ln 4f`。
2. 是否需要为 `embedded_active_aolabels` 提供默认值，还是继续要求用户显式指定。
3. 是否需要为环境 core/virtual 边界增加 gap 诊断和 warning。
4. 是否需要把 example 中的裸 `raise` 改成更干净的诊断开关。
5. fragment density 拼接方案是否需要包含 ligand-ligand cross block 的近似修正。
