# Coding Log 20260508 1114

## 背景

本次记录对应 fragment-based DMET 的第一轮框架搭建。当前目标是为 `examples/fragment_dmet.py` 中的 `fragment.FDMET` 提供一个清晰的代码入口，并先固定 fragment-DMET 的输入约定：impurity 和 ligand fragments 用 PySCF AO label/pattern 定义，内部统一解析为 AO index。

## 当前结果

1. 新增 `embed_sim/fragment.py`。
2. 新增 `FDMET` 类，并让它继承 `ssdmet.SSDMET`。
3. `FDMET` 接收 PySCF `Mole` 对象，不接收全局 mean-field/CASSCF reference 作为 bath 构造来源。
4. `FDMET` 只保存 molecule 和 AO-index 分片信息，不自动构造或消费全局 SCF reference。
5. `FDMET.build()` 进入 fragment bath 路径。该路径目前显式抛出 `NotImplementedError`，避免错误地根据全局 SCF density 构造 bath orbital。
6. 未实现的功能直接报错，不保留备用 bath 构造路径。
7. `FDMET` 的核心输入使用 AO label/pattern：
   - `imp_idx` 直接继承 `SSDMET` 的 property 和 setter。
   - `fragments` 是若干 fragment label 定义，每个 fragment 会用 PySCF AO label parser 解析成 AO index。
8. 在 `fragment.py` 中加入 fragment label 解析结果的校验：
   - 输入不能为空。
   - 输入不能包含重复 AO index。
   - 输入不能超出 `[0, mol.nao)` 范围。
9. `fragment.py` 不解释 fragment 名称、原子编号或字典结构；它只解析 AO label/pattern，并统一保存为 AO index。
10. `FDMET` 保留 `fragment_scf` 参数，用于后续选择 fragment-local SCF/CAHF 类型。

## Example 状态

`examples/fragment_dmet.py` 已经为 `Co(SH)_4` 定义 fragment：

1. impurity 是 Co center。
2. `L1` 到 `L4` 分别是四个 SH ligand fragment。
3. 输入脚本中用字典组织可读的分片定义。
4. 当前 example 直接把 AO label/pattern 传给 `FDMET`：

```python
mydmet = fragment.FDMET(
    mol,
    title=title,
    imp_idx=impurity['aolabels'],
    fragments=fragments,
)
```

其中 `fragments` 是：

```python
fragments = [ligand['aolabels'] for ligand in ligands]
```

## Basis 约定

当前实现明确区分两层输入：

1. 用户脚本层可以使用化学名称、原子分组或字典来帮助组织输入。
2. `embed_sim/fragment.py` 代码层接受 AO label/pattern，并统一解析为 AO index。

后续 fragment-local bath builder 应从每个局部空间

$$
\mathcal{I}\oplus\mathcal{L}_f
$$

的 reference density 构造 bath orbital，而不是使用完整体系的全局 SCF density。最终输出仍应保持为全局 AO coefficient，例如 `es_orb`、`fo_orb` 和 `fv_orb`，以便继续复用现有 Hamiltonian projection、AVAS、`total_cas` 和 SISO 接口。

## 剩余问题

1. 实现 `build_fragment()`，从解析后的 AO index 定义构造每个 $\mathcal{I}\oplus\mathcal{L}_f$ 局部空间。
2. 确定 `fragment_scf` 支持的具体类型，优先考虑 CAHF，并保留 ROHF 作为最小可调试路径。
3. 定义 fragment 子体系的电荷、自旋和电子数。
4. 决定局部空间中的 overlap、one-electron integral 和 two-electron integral 如何由全局 AO integral 投影得到。
5. 实现从 fragment-local density 的环境块对角化得到 bath orbital 的构造流程。
6. 实现多个 fragment bath orbital 合并后的 overlap-metric 正交化和线性相关检查。

## 已运行检查

```bash
conda run -n pyscf python -m py_compile embed_sim/fragment.py examples/fragment_dmet.py
```

结果：通过。

```bash
conda run -n pyscf python -c "from pyscf import gto; from embed_sim import fragment; mol=gto.M(atom='H 0 0 0; H 0 0 0.74', basis='sto-3g', verbose=0); d=fragment.FDMET(mol, imp_idx='0 H 1s', fragments=['1 H 1s']); print(d.imp_idx, d.fragments)"
```

结果：通过。确认 `FDMET` 可以接收 AO label/pattern 形式的 impurity 与 fragment。
