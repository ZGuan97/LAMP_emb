# Coding Log 2026-05-15 00:20

## 本次目标

本次 session 开始实现 fragment-DMET 的 `build_fragment` 路径。当前阶段完成第一步：对每个 ligand fragment，构造 `impurity + ligand_f` 的局部 `Mole` 对象，并在该局部子体系上运行 fragment-local CAHF reference。bath orbital 构造、density 投影和 embedded cluster 合并仍保留为后续工作。

## 最终接口状态

`FDMET` 当前接收 fragment 分片和分片电荷：

```python
mydmet = fragment.FDMET(
    mol,
    title=title,
    imp_idx=impurity["aolabels"],
    imp_charge=impurity["charge"],
    fragments=fragments,
    fragment_charges=fragment_charges,
    fragment_scf="cahf",
    fragment_scf_options={
        "ncas": 5,
        "nelecas": 7,
        "cahf_spin": 3,
        "diis": "rdiis",
        "rdiis_prop": "dS",
        "rdiis_imp_idx": ["Co.*d"],
        "rdiis_power": 0.2,
        "max_cycle": 200,
        "level_shift": 0.5,
    },
)
mydmet.build(fragment_scf_verbose=3)
```

约定如下：

1. `imp_idx` 和 `fragments` 继续使用 PySCF AO label/pattern 定义。
2. `imp_charge` 表示 impurity fragment 的电荷。
3. `fragment_charges[f]` 表示第 `f` 个 ligand fragment 的电荷。
4. 第 `f` 个局部 molecule 的总电荷取 `imp_charge + fragment_charges[f]`。
5. `fragment_scf_verbose` 只作为 `build()` 或 `build_fragment()` 的参数出现，不属于 `FDMET` 构造器参数。
6. `fragment_scf_verbose=3` 时默认静音 RDIIS entropy 输出；`fragment_scf_verbose=4` 时输出。用户仍可通过 `rdiis_mute` 显式覆盖。

底层函数也可直接使用：

```python
ref = fragment.run_fragment_scf(
    mol,
    impurity_idx,
    fragment_idx,
    fragment_scf="cahf",
    charge=1,
    ncas=5,
    nelecas=7,
    cahf_spin=3,
    diis="rdiis",
    rdiis_imp_idx=["Co.*d"],
    fragment_scf_verbose=3,
)
```

底层 `run_fragment_scf()` 将局部总电荷 `charge` 作为显式函数参数接收，而不是从可选 `kwargs` 中读取。`imp_charge + fragment_charges[f]` 的求和逻辑只保留在 `FDMET.build_fragment()` 中。

## 代码改动

1. 在 `embed_sim/fragment.py` 中新增 `FragmentReference` 数据容器，用于保存一个 impurity-ligand fragment reference：
   - 局部 `mol`
   - 局部 `mf`
   - 该 fragment 涉及的全局 atom/AO index
   - impurity 与 ligand fragment 在局部 AO basis 中的 index 映射

2. 新增 fragment molecule 构造逻辑：
   - 根据全局 `impurity_idx + fragment_idx` 找到涉及的原子。
   - 从母体 `Mole` 复制这些原子的元素、Bohr 坐标、basis、ECP、cart 设置和 memory 设置。
   - 构造截断的 `fragment_mol`，默认关闭 symmetry。
   - 按 atom 内 AO 顺序建立全局 AO 到局部 AO 的映射，避免依赖 AO label 字符串反查。

3. charge 处理改为显式输入：
   - `_make_fragment_mol()` 不再推断 charge。
   - 没有显式 charge 时直接报错：

```python
ValueError("fragment molecule charge must be assigned explicitly")
```

4. 实现 `run_fragment_scf()`：
   - 支持 `fragment_scf="cahf"`，调用 `embed_sim.cahf.CAHF(...).x2c()`。
   - 保留 `fragment_scf="rohf"` 作为调试路径。
   - CAHF 默认参数暂设为 `ncas=5, nelecas=7`，对应当前 Co d7 示例。
   - 支持 `charge`、`spin`、`ncas`、`nelecas`、`cahf_spin`、`max_cycle`、`level_shift`、`max_memory` 等参数。

5. 新增 `build_fragment()` 函数，作为构造单个 impurity-ligand fragment reference 的显式入口。

6. 更新 `FDMET.build()` 和 `FDMET.build_fragment()`：
   - `build(fragment_scf_verbose=3)` 进入 fragment bath 路径。
   - 逐个 ligand fragment 构造 `impurity + ligand_f` 局部子体系。
   - 对每个子体系运行 fragment-local CAHF。
   - 将结果保存到 `self.fragment_refs`。
   - bath construction 尚未实现的位置继续显式抛出 `NotImplementedError`。

7. 将 `FDMET` 默认 `fragment_scf` 从 `rohf` 改为 `cahf`，以符合当前 fragment-DMET 设计目标。

8. 新增 fragment-local RDIIS 接口：
   - `diis="rdiis"` 时，在局部 `fragment_mol` 构造完成后创建 `embed_sim.rdiis.RDIIS`。
   - `rdiis_imp_idx` 在局部 molecule 中解析，因此可直接使用 `["Co.*d"]` 这样的 AO label/pattern。
   - 支持 `rdiis_prop`、`rdiis_power`、`rdiis_kernel`、`rdiis_mute`、`rdiis_ent_conv_tol` 和 `rdiis_space`。
   - 默认 `rdiis_mute` 根据 `fragment_scf_verbose` 决定。

9. 更新 `examples/fragment_dmet.py`：
   - 为 Co impurity 加入 `charge=2`。
   - 为四个 SH ligand fragment 加入 `charge=-1`。
   - 加入 `fragment_scf_options`，参考 `examples/CAHF.py` 设置 CAHF 和 RDIIS。
   - 将局部 SCF verbose 写到 `mydmet.build(fragment_scf_verbose=3)`。

10. 更新 `fragment.md`：
   - 记录当前 fragment-local CAHF 第一步实现状态。
   - 记录显式 charge 输入约定。
   - 记录 RDIIS 和 `fragment_scf_verbose` 行为。

11. 精简 fragment scaffold：
   - 将 `_validate_fragments()` 简化为 `_parse_fragments()`。
   - 删除 fragment atom 为空的额外检查。
   - 删除 `set_fragments()` 和 `set_fragment_charges()`，当前阶段只保留构造时传入 fragments 和 charges 的路径。
   - 删除 `fragment_charges requires fragments` 的初始化检查，让 build 阶段统一要求 `imp_charge` 和 `fragment_charges` 同时存在。

## 当前假设

1. 当前默认 CAHF active space 是 Co d7 示例用的 `ncas=5, nelecas=7`，不是通用默认值。更一般体系应显式传入 `fragment_scf_options`。
2. fragment charge 必须显式输入；代码不做自动推断。
3. fragment 子体系目前是直接截断 molecule，没有添加 link atom 或 embedding potential。
4. 当前每个 fragment-local CAHF 只包含 `impurity + ligand_f` 局部 molecule。其他 ligand fragment 的核吸引势、电子库仑势、净负电荷静电场和轨道极化影响尚未进入该局部 reference。
5. `fragment_charges` 当前只决定对应局部 molecule 的电子数；还没有作为 point charge、embedding potential 或全局投影势作用到其他 fragment 的局部参考计算中。
6. 局部 AO index 和母体全局 AO index 是不同编号系统。当前代码保存 `atom_ids`、`global_idx`、`local_imp_idx` 和 `local_fragment_idx` 来记录两者关系；局部 AO 到全局 AO 的对应按 atom 内 AO 顺序建立。
7. 当前实现只构造 reference wavefunction；density 投影、固定 impurity 正交化、bath orbital 构造和最终 embedded cluster 合并仍未实现。

## 验证

已运行：

```bash
conda run -n pyscf python -m py_compile embed_sim/fragment.py examples/fragment_dmet.py
```

结果：通过。

小 CoSH/STO-3G sanity check 覆盖了以下行为：

1. `FDMET` 中显式 `imp_charge=2` 和 `fragment_charges[f]=-1` 会构造局部总电荷为 +1 的 fragment。
2. 局部 fragment 得到 `charge=1, nelectron=43, spin=3`。
3. 不传 charge 时会按预期报错。
4. `diis="rdiis"` 会创建 `RDIIS` 对象。
5. `rdiis_imp_idx=["Co.*d"]` 会在局部 molecule 中解析为 Co d 轨道 index。
6. `fragment_scf_verbose=3/4` 时，RDIIS 的默认 `mute` 分别为 `True/False`。

## 后续工作

1. 明确不同体系中 fragment charge/spin 的物理定义，尤其是带电或共价配体。
2. 决定其他 ligand fragment 的电荷和静电场是否需要以 point charge、embedding potential 或全局 AO Hamiltonian 投影的形式进入每个局部 CAHF。
3. 从每个 fragment CAHF reference 取一体密度矩阵，并明确它所在的 AO/local orthogonal basis。
4. 复用现有 `preserve_imp` 思路，实现固定 impurity 的 fragment-local 正交化。
5. 从 fragment density 中构造 bath orbital。
6. 合并所有 fragment bath，形成全局 embedded cluster space。
7. 对接现有 `SSDMET` 的 Hamiltonian projection、AVAS 和 solver 接口。
